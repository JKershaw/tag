{
  "objective": "Assess this real MangoDB document lifecycle check. State whether the supplied observations meet the expected insert/update/delete/reopen behavior, cite the host test, and limit the conclusion to this case. Do not claim you ran tools.",
  "context": "Trusted-host observation from test/mangodb-tasks.test.js, lifecycle case, using @jkershaw/mangodb and an isolated temporary directory: inserted a={count:2} and b={count:7}; updateOne on a with $inc count by 3 matched 1 and modified 1; deleteOne on b deleted 1. After closing and opening a new MangoClient, findOne(a).count was 5, findOne(b) was null, and countDocuments was 1. Expected: a.count=5, b absent, one remaining document. This is one observed local case, not a MongoDB compatibility certification.",
  "limits": {
    "maxGenerations": 2,
    "maxCostUsd": 0.03,
    "concurrency": 1,
    "maxRetries": 0,
    "minDelayMs": 3000,
    "maxOutputTokens": 1536,
    "maxNodes": 3
  }
}
